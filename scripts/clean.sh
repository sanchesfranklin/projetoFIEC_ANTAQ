# remove todos os arquivos .txt / .zip do diretório de landing
rm -f data/landing/Atracacao/*.zip
rm -f data/landing/Carga/*.zip

# removendo os txt descompactados
rm -f data/landing/Atracacao/unzip/*.txt
rm -f data/landing/Carga//unzip/*.txt